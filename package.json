{
  "name": "fly-arc-visualizer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "format": "prettier --write \"**/*.{js,ts,jsx,tsx,json,css,scss,md,html}\""
  },
  "dependencies": {
    "maplibre-gl": "6.0.0-13"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.8.4",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
