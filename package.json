{
  "name": "fly-arc-visualizer",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "format": "prettier --write \"**/*.{js,ts,jsx,tsx,json,css,scss,md,html}\""
  },
  "dependencies": {
    "maplibre-gl": "file:../maplibre-gl-js"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "prettier": "^2.8.8",
    "typescript": "~5.5.0",
    "vite": "^5.4.0"
  }
}
